const pages=[...document.querySelectorAll('article')];const links=[...document.querySelectorAll('nav a')];function show(){let id=location.hash.slice(1)||'start';if(!pages.some(p=>p.id===id))id='start';pages.forEach(p=>p.hidden=p.id!==id);links.forEach(a=>{a.classList.toggle('active',a.dataset.page===id);a.toggleAttribute('aria-current',a.dataset.page===id)});document.title=pages.find(p=>p.id===id).querySelector('h1').textContent+' | Meld Clay guide';window.scrollTo({top:0,behavior:'instant'});requestAnimationFrame(()=>window.scrollTo({top:0,behavior:'instant'}))}window.addEventListener('hashchange',show);window.addEventListener('popstate',show);document.addEventListener('click',e=>{let a=e.target.closest('a[href^="#"]');if(a){e.preventDefault();history.pushState(null,'',a.hash);show()}});show();document.querySelector('#search').addEventListener('input',e=>{let q=e.target.value.trim().toLowerCase();let count=0;links.forEach(a=>{let visible=!q||document.getElementById(a.dataset.page).textContent.toLowerCase().includes(q);a.parentElement.hidden=!visible;if(visible)count++});document.querySelector('#empty').hidden=count>0});